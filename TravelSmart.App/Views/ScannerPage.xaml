<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI.Controls"
             x:Class="TravelSmart.App.Views.ScannerPage"
             Title="Quét mã QR/NFC"
             BackgroundColor="Black"
             NavigationPage.HasNavigationBar="False">

    <Grid>
        <zxing:CameraBarcodeReaderView x:Name="BarcodeReader"
                                       BarcodesDetected="BarcodesDetected"
                                       IsDetecting="True" />

        <Border Stroke="White" StrokeThickness="4" 
                WidthRequest="250" HeightRequest="250"
                HorizontalOptions="Center" VerticalOptions="Center"
                BackgroundColor="Transparent" />

        <Label Text="Đưa mã QR Quán ăn vào khung hình" 
               TextColor="White" FontAttributes="Bold" FontSize="16"
               HorizontalOptions="Center" VerticalOptions="End" Margin="0,0,0,50" />

        <Button Text="✖ Đóng" BackgroundColor="Red" TextColor="White" 
                HorizontalOptions="Start" VerticalOptions="Start" 
                Margin="20,40,0,0" Clicked="OnCloseClicked"/>
    </Grid>
</ContentPage>