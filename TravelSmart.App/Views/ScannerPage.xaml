<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI.Controls"
             x:Class="TravelSmart.App.Views.ScannerPage"
             Title="Quét mã QR/NFC"
             BackgroundColor="Black"
             NavigationPage.HasNavigationBar="False">

    <Grid>
        <zxing:CameraBarcodeReaderView x:Name="BarcodeReader"
                                       BarcodesDetected="BarcodesDetected"
                                       IsDetecting="True" />

        <Border Stroke="White" StrokeThickness="4" 
                WidthRequest="250" HeightRequest="250"
                HorizontalOptions="Center" VerticalOptions="Center"
                BackgroundColor="Transparent" />

        <Label Text="Đưa mã QR Quán ăn vào khung hình" 
               TextColor="White" FontAttributes="Bold" FontSize="16"
               HorizontalOptions="Center" VerticalOptions="End" Margin="0,0,0,100" />

        <HorizontalStackLayout HorizontalOptions="Center" VerticalOptions="End" Margin="0,0,0,30" Spacing="20">
            <Button Text="🖼 Tải ảnh lên" BackgroundColor="#2196F3" TextColor="White" FontAttributes="Bold" CornerRadius="8" Padding="15,10" Clicked="OnUploadClicked"/>
            <Button Text="✖ Đóng" BackgroundColor="#D32F2F" TextColor="White" FontAttributes="Bold" CornerRadius="8" Padding="15,10" Clicked="OnCloseClicked"/>
        </HorizontalStackLayout>

        <BoxView x:Name="LoadingOverlay" BackgroundColor="Black" Opacity="0.7" IsVisible="False"/>
        <VerticalStackLayout x:Name="LoadingIndicator" VerticalOptions="Center" HorizontalOptions="Center" IsVisible="False" Spacing="10">
            <ActivityIndicator IsRunning="True" Color="White" />
            <Label Text="Đang giải mã ảnh..." TextColor="White" FontAttributes="Bold" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>